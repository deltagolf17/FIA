import { WeatherMapShell } from "@/components/weather-map/WeatherMapShell";

export const metadata = { title: "Weather Map — FireTrace Pro" };

export default function WeatherMapPage() {
  return <WeatherMapShell />;
}
